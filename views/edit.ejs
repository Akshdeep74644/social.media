<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-gray-900">
    
<div class="w-full p-10  h-full text-[#dedede]  bg-gray-900 ">
  <div class="w-full flex justify-between items-center">
    <h1 class="text-2xl">Hello, <%= post.user.username %> 👋🏼 </h1>
    <a href="/logout" class="p-2 rounded-lg bg-red-800">Logout</a>
  </div>

  <div class="mt-[5%]">
    <form action="/updatepost/<%= post._id %>" class="flex flex-col gap-5" method="post">
      <h1 class="text-{#dedede} text-4xl">Edit your post</h1>
      <textarea name="content"  class="resize-none outline-none w-[60%] h-25 border-2 border-zinc-400 rounded-2xl p-2" placeholder="type..."><%= post.content %></textarea>
      <button type="submit" class="text-white w-[10%] bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Update it</button>
    
    </form>
  </div>
</div>
  </body>
</html>